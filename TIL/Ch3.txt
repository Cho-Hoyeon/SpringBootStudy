Spring Boot 3 계층

컨트롤러 - 서비스 - 리포지터리
(프레젠테이션) (비즈니스) (퍼시스턴스) <-> DB

프레젠테이션 계층 : HTTP 요청을 받아서 비즈니스 계층으로 전송
비즈니스 계층  : 내부 로직
퍼시스턴스 계층 : DB 관련 로직

Spring의 요청-응답 과정

HTTP 요청 받음 - 컨트롤러가 URL 분석 및 요청 가져옴 - 프레젠테이션 계층이 타 계층에서 데이터 가져옴
- 뷰 리졸버가 HTML 문서나 JSON, XML 등의 데이터를 생성 - 디스패치 서블릿에 의해 응답으로 클라이언트에 반환됨